{
  "name": "even-stopwatch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cp index.html dist/ && esbuild src/main.ts --bundle --outdir=dist --format=esm --servedir=dist --watch",
    "build": "cp index.html dist/ && esbuild src/main.ts --bundle --outdir=dist --format=esm --minify"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "^0.0.7"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
